server:
  port: 8080
spring:
  application:
    name: student
  datasource:
    url: jdbc:mysql://localhost:3307/microservices?useSSL=false&serverTimezone=UTC
    username: root
    password: root1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true